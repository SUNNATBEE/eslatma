# routes/ — API endpointlari domenlar bo'yicha ajratilgan
# Har bir modul setup_X_routes(app, ctx) funksiyasini eksport qiladi
# ctx = {bot, db, tz, auth, admin_auth, mini_admin_auth, notify_level_up, get_user_id}
